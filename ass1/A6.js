function numRollstogetsix(){
res=Math.floor(Math.random()*7);
    var count=0;
    if(res!=6)
        {
            count=count+1;
        }
 console.log(res);
    console.log(count);
}

numRollstogetsix();